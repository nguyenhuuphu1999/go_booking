import React from 'react';

const Icon = (props) => {
    const handlPlus =()=>{
            //(2)
    }
    return (
        < >
            <props.icon style={{marginTop:"-28px", borderRadius:"-18px"  }} onclick={handlPlus}/>
        </>
    );
};

export default Icon;